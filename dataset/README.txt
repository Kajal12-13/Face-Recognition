This folder stores the face images for training the face recognition system.

Do NOT upload real images here in your GitHub repository.

Each person's images should be placed inside a separate folder:
Example:
dataset/Alice/
dataset/Bob/

You can automatically capture images using:
python src/enroll.py --name "<person_name>"

This folder will be ignored by .gitignore for privacy and security reasons.
